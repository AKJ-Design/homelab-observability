# Secrets — never commit real credentials
.env                    # Bitcoin RPC user/password
prometheus/*.key        # Uptime Kuma API key (prometheus/uptime_kuma.key)

# OS cruft
.DS_Store

# Grafana/Prometheus runtime data live in named Docker volumes, not here,
# but ignore these just in case anyone bind-mounts locally.
data/
*.log
